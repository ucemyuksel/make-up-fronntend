import { auth } from "../../auth";
import { redirect } from "next/navigation";
export default async function Content() { const s = await auth() as { roles?: string[] } | null; if (!s?.roles?.includes("ADMIN")) redirect("/yetkisiz"); return <main style={{ maxWidth: 760, margin: "0 auto", padding: 32 }}><a href="/">← Yönetim merkezi</a><h1>Paylaşım ve video dağıtımı</h1><section className="gg-card"><strong>Önerilen işlem türleri</strong><p>Paylaşımı kaldırma, paylaşım yapmayı geçici kısıtlama, yorumları kapatma ve video dağıtımını <em>normal / sınırlı / önerilmez</em> seviyelerine alma.</p><p>Görünürlük azaltma yalnızca politika ihlali, spam veya güvenlik riski gibi gerekçeli durumlarda; neden, süre ve itiraz hakkı ile uygulanmalıdır. Ayrı bir reels/post moderasyon API’si henüz bulunmadığından bu sayfa karar kayıtlarını işlemeye bağlanmamıştır.</p></section></main>; }
