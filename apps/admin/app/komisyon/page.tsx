import { auth } from "../../auth";
import { redirect } from "next/navigation";
export default async function Commission() { const s = await auth() as { roles?: string[] } | null; if (!s?.roles?.includes("ADMIN")) redirect("/yetkisiz"); return <main style={{ maxWidth: 760, margin: "0 auto", padding: 32 }}><a href="/">← Yönetim merkezi</a><h1>Komisyon ayarları</h1><section className="gg-card"><strong>Tarif satışları · %15 platform komisyonu</strong><p>Sanatçıya net %85 aktarılır. Bu oran satın alma servisinin ortam ayarından yönetilir: <code>PURCHASE_REVENUE_ARTIST_SHARE=0.85</code>.</p><p style={{ color: "var(--gg-muted)" }}>Canlı oran değişikliği için denetlenebilir, sürümlü bir ayar servisi eklenmelidir; doğrudan panelden değiştirmek henüz etkin değildir.</p></section></main>; }
